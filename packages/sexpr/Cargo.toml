[package]
name = "sexpr-package"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
packr-guest = { path = "../../crates/pack-guest" }
packr-abi = { path = "../../crates/pack-abi", default-features = false }

[profile.release]
opt-level = "s"
lto = true
